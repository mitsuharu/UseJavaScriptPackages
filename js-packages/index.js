import mean from "just-mean"
import random from "just-random"

export class Bridge {

    static mean(array) {
        return mean(array)
    }

    static random(array) {
        return random(array)
    }
}